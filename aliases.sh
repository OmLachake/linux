# File for all the Aliases that I use on 
# the daily.

# Linux
alias ll="ls -alF"
alias c="clear"
alias zsh="source ~/.zshrc"

# Package Management
alias update="sudo apt update && sudo apt upgrade"

# Git 

alias gi="git init"
alias ga="git add"

alias gs="git status"
alias gc="git commit -m "

alias gps="git push"
alias gpl="git pull"

alias gb="git branch"
alias gbc="git checkout -b"
alias gbd="git branch -D"

alias glo="git log --oneline --decorate"
alias gl="git log"
